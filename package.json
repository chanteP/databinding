{
  "name": "databind",
  "version": "0.0.0",
  "description": "",
  "main": "core.js",
  "scripts": {
    "preinstall": "npm install -g grunt-cli",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies" : {
    "grunt-browserify" : ">=0.0.0"
  },
  "author": "",
  "license": "ISC"
}
